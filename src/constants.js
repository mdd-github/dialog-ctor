export const CHARACTER_CARD_MODE = Object.freeze({
    VIEW: 'view',
    EDIT: 'edit',
});

export const THREAD_MESSAGE_MODE = Object.freeze({
    VIEW: 'view',
    EDIT: 'edit',
});

export const AUTHOR_TYPE = Object.freeze({
    USER: 'user',
    CHARACTER: 'character'
});

export const QUESTION_TYPE = Object.freeze({
    TEXT: 'text',
    SELECT: 'select',
    NONE: 'none'
});